fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
geoalchemy2==0.16.0
psycopg[binary]==3.2.3
alembic==1.14.0
pydantic==2.10.4
pydantic-settings==2.7.1
python-dotenv==1.0.1
anthropic==0.116.0

# Dipakai fastapi.testclient di backend/tests/
httpx==0.28.1

# --- Akun dan langganan ----------------------------------------------------
# Autentikasi TIDAK menambah dependensi: kata sandi disidik hashlib.scrypt dan
# tiket sesi ditandatangani hmac, keduanya dari pustaka standar. Lihat alasannya
# di app/core/akun.py.
#
# Yang di bawah ini bukan untuk auth:
# validasi bentuk surel saat mendaftar (pydantic EmailStr)
email-validator==2.2.0
# Laporan Kelayakan (PDF). Pure Python - tidak butuh kompilasi di Windows.
reportlab==4.2.5
