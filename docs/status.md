# Status proyek

Dipisah dari `CLAUDE.md` supaya tidak dibayar tiap sesi. **Isinya tidak diubah
sedikit pun** — ini catatan yang sama, di tempat yang lebih murah.

Baca ini kalau Anda perlu tahu apa yang sudah jadi, apa buktinya, dan apa
pekerjaan berikutnya. Kalau Anda cuma akan menyentuh kode, `CLAUDE.md` sudah
cukup.

---

## Status per 21 Agustus 2026 (backend lengkap)

### Sudah selesai dan terverifikasi

| Bagian | Bukti |
|---|---|
| Skema basis data | 50 kolom di `hex_features` = 43 variabel + 3 penanda + kunci/geom/waktu, plus `hex_hourly_profiles`. Migrasi diterapkan ke Supabase |
| 7 modul API, 46 rute | Smoke test 95 asersi ke Supabase dalam transaksi yang di-rollback (0 baris tersisa) |
| Keenam fitur produk | PriceLens · AI Consultant · Commuter Clock · ZoneGuard · RiskRadar · GemFinder |
| AI Consultant | 12 alat mode strict, loop agentik, 26 asersi dengan klien tiruan |
| **AI Consultant multi-penyedia** | `backend/app/core/llm_gemini.py` (3 Sep 2026) — adapter Gemini di belakang kontrak yang sama dengan klien Anthropic (`.messages.create()` berbentuk sama), dipilih lewat `LLM_PROVIDER=gemini` tanpa menyentuh `api/ai.py`. Menerjemahkan skema alat JSON Schema→OpenAPI, menangkap `thoughtSignature` wajib Gemini, dan jatuh ke `MODEL_CADANGAN` kalau model utamanya 503/429/404. Diuji end-to-end lewat `/ai/tanya` sungguhan: 8 panggilan alat, 3 aksi peta, jawaban menyebut angka basis data
| Mesin skoring | 14/14 uji lolos. Sensitivitas ρ 0,9719–0,9919 |
| Commuter Clock & PriceLens (pipeline) | 13/13 uji lolos |
| Prompt A1–A4 | Prompt produksi, sudah cocok dengan skema Pydantic |
| Frontend | 3 bagian wajib + sistem visual, Kompas Kuadran, daftar berdasar layer, 3 grafik. Peta memperlihatkan POI basemap sejak z12,5 supaya tetap terbaca sebagai peta; heksagon diturunkan opasitasnya ~30% dan dikompensasi garis yang lebih tebal. Heksagon terpilih & yang dibandingkan masuk **mode fokus**: isiannya dicabut lewat faktor pengali ketiga di ekspresi `fill-opacity`, garisnya ditebalkan, dan yang dibandingkan diberi lencana bernomor + tali putus-putus berlabel jarak |
| Halaman gerbang | Scrollytelling GSAP, sepuluh bagian, hampir semuanya setinggi layar. **Nol MapLibre, nol WebGL** — peta aplikasi baru dipasang saat gerbang ditutup. Latar hero: sarang lebah satu `<pattern>` dengan **lensa yang mengikuti kursor**. Hero memuat **ikhtisar peta MapLibre sungguhan** — 108 heksagon Tanah Abang dari `/hex/layer`, basemap MAPID Dasar, DATAR dan tegak lurus dari atas (tanpa pitch, tanpa bearing; kartunya pun rata sampai orangnya menggulir), dibingkai `fitBounds` dengan bantalan proporsional. Dek enam kartu di `GerbangPeta.tsx`: enam kawasan, lima layer, empat gaya basemap MAPID — semuanya **`<img>` WebP statis** (~210 KB) yang dibuat `scripts/potret-kartu.mjs` dari basis data sungguhan dengan ekspresi pewarnaan yang sama dengan aplikasi. Halaman gerbang **tidak memuat MapLibre sama sekali**. Mengklik kartu membuka aplikasi pada kawasan dan layer itu. Bagian kuadran memakai **gulir lintang** (pin + geser samping + panel saling menimpa); pipeline memakai **rel cairan** (gumpalan yang melebur dengan simpul, filter `#g-lengket`); bagian tim ada di paling bawah di balik **jurang** yang menggelap ke hitam. Urutan layar: **gerbang → pembuka → peta** |
| Data demo | `pipeline/demo_seed.py` — 708 heksagon lewat pipeline sungguhan. Kuadran jatuh persis seperti dugaan PRD. **Seluruhnya `predicted`/`RENDAH`/0 titik misi**, dan pita "Data demo" di bilah atas menyatakannya — belum ada satu pun observasi misi MAPID di basis data |
| **Data demo pameran (dapat dicopot)** | `pipeline/demo_pameran.py` (3 Sep 2026) — beda dari `demo_seed.py`: MENAMBAL sel kosong di basis data yang SUDAH berisi data sungguhan, bukan membangun dari nol. Angka diturunkan dari sinyal nyata (jarak simpul, WorldPop, POI), skor dihitung ulang lewat `s7_publish.hitung_ulang_dari_db()` (aturan 1 tetap utuh). `--copot` mengembalikan basis data PERSIS seperti semula — dibuktikan lewat sidik jari (total skor, sebaran kuadran, jumlah baris tiap tabel) sebelum/sesudah round-trip isi→copot→isi, hasilnya identik. Dipakai untuk pameran 3-4 September 2026; TIDAK untuk dibiarkan terpasang permanen — gate page (`ringkasan-data.ts`) sengaja TIDAK diregenerasi dengan angka demo, supaya klaim metodologi ke juri tetap jujur (25/708 heksagon benar-benar disurvei lapangan)
| Ketahanan produksi | Amplop galat, cache TTL, pembatas laju, plafon biaya, GZip, kompresi geometri, bbox |
| Jembatan pipeline → DB | `s7_publish.py` — muat ke basis data + ekspor GeoJSON statis untuk CDN |
| Akun & Loconomics Premium | 5 tabel, 13 rute `/akun`, 3 rute premium di `/skor`. Tiga tingkat (tamu/gratis/premium), token satuan, Laporan Kelayakan PDF. 44 asersi di `test_akun.py` — sebagian besar menguji bahwa yang berbayar TIDAK keluar |
| Keenam baris tabel fitur | Multi-kawasan · riwayat skor · 43 variabel granular · komparasi berdampingan · pemantauan + dinamika kawasan · PDF Export |
| **Komparasi matang** | Baki 2-4 heksagon dengan bar sendiri di tengah bawah, terbagi rata sebanyak yang dipilih. Nomor kolomnya SAMA dengan lencana di peta. Heksagon yang dibandingkan masuk mode fokus: isian hilang, garis tebal. Dialognya memakai bar per metrik dengan arah yang sudah diperhitungkan - sewa termurah tampil sebagai bar TERPANJANG - plus ekspor PDF mendatar |
| **Mode fokus peta** | Heksagon terpilih dan yang dibandingkan isiannya dikalikan NOL lewat ekspresi opasitas, bukan lewat `filter` (filter layer isian sudah dipakai saringan kuadran dan saringan AI). Garis putus-putus ke stasiun terdekat dengan jarak dan menit - GARIS, bukan lingkaran; `catchment_areas` masih kosong |
| **Simulasi jadi slider** | Empat slide ber-`scroll-snap`, satu pertanyaan per slide, penggeser asumsi menetap di kaki. Sebelumnya empat kolom berdesakan dalam 46vh dan tidak ada satu pun yang cukup ruang untuk dibaca |
| **Rekomendasi personal** | `/skor/rekomendasi` + tab "Untuk Anda". Menyaring menurut anggaran & kawasan preferensi, lalu menjelaskan tiap baris dengan angka lokasi itu sendiri. Akun gratis melihat 3 teratas dan diberi tahu berapa lagi yang cocok - `total_cocok` selalu angka sebenarnya. **Tidak menghitung ulang skor**: urutannya tetap `opportunity_score` milik pipeline |
| Onboarding usaha | `users.preferensi` (JSON teks) diisi sesudah langganan aktif: jenis usaha, kawasan incaran, anggaran sewa. Menyetel bawaan simulasi dan memindahkan peta. TIDAK menyentuh satu pun skor |
| Lokasi tersimpan | Pin `Marker` DOM di peta, disegarkan lewat `sinyalSimpan` di konteks sesi - bukan menunggu refresh. Bisa dikirim langsung ke baki komparasi |
| Simulasi matang | Pangsa impas (angka paling layak dipercaya - tanpa asumsi pangsa sama sekali), tabel kepekaan pada 4 nilai pangsa, sewa tahun pertama, peringatan kalau pangsa impasnya di atas 25% |
| **Rute jalan kaki (ORS)** | `hex_routes` — 1.587 rute untuk 708 heksagon, nol gagal. Rata-rata memutar **1,82x** dari jarak lurusnya, dan 470 dari 708 heksagon memutar ≥1,4x. Dihitung `pipeline/rute_ors.py`, dibaca `/hex/{h3}/simpul-terdekat`. Garis lurus putus-putus DICABUT dari peta |
| **Kawasan jangkau (isochrone)** | `catchment_areas` — 18 pita untuk 6 simpul, lewat ORS `/v2/isochrones`. Luasnya 42–67% dari lingkaran naif, jadi bentuknya memang dibatasi jaringan jalan. Manggarai menjangkau **17 heksagon dalam 15 menit** sementara stasiun lain 34–41 — emplasemen relnya memotong jangkauan ke segala arah. Digambar sebagai GARIS berlabel di peta, di atas heksagon dan di bawah rute |
| **Mode gelap halaman gerbang** | Palet gerbang jadi variabel `--g-*`; mode gelap memakai warna dasar jurang di bagian tim. `.gerbang[data-tema='gelap']` dan `.g-nav-gelap` IKUT selektor token gelap `.peta-gelap`, jadi komponen aplikasi yang dipakai ulang (Kompas Kuadran, tombol akun, kartu kaca) ikut benar tanpa ditambal satu per satu. Sakelar cair di hero menggantikan paragraf autentikasi yang sudah tidak benar sejak akun masuk |
| **Pembatas + komposisi heksagon per bagian** | Tiap perbatasan diberi garis rambut yang memudar di kedua ujung dengan heksagon kecil di tengahnya, digambar lewat `scaleX` saat masuk layar. Tujuh bagian punya KOMPOSISI heksagonnya sendiri di belakangnya - semuanya heksagon bergaris seperti terowongan di dasar jurang, tetapi SUSUNANNYA mengacu pada isi bagiannya: sarang enam-mengelilingi-satu untuk enam kawasan, heksagon terbelah dua sumbu untuk Kompas Kuadran, rantai menurun untuk pipeline, empat heksagon meninggi untuk empat angka. Digerakkan SATU pendengar gulir: `scale` naik, `rotate` berjalan, opasitas memuncak saat bagiannya di tengah layar. Posisinya mengikuti tengah LAYAR, bukan tengah bagian |
| **Latar hero: kisi heksagon yang menyala mengikuti kursor** | Sarang redup yang DIAM, plus jendela bundar yang mengikuti kursor berisi sarang terang yang digeser BERLAWANAN sejauh yang sama - jadi sarang terang itu diam terhadap halaman dan menimpa sarang redup persis heksagon demi heksagon. Yang bergerak cuma jendelanya; keduanya `transform`, nol piksel dilukis ulang. Tidak ada gerak sendiri sama sekali: tidak bernafas, tidak berdenyut, tidak beriak. Elemennya anak `absolute` DI DALAM hero yang sudah `overflow-hidden`, jadi ia tidak bisa bocor ke bagian mana pun di bawahnya. Terukur di produksi: bingkai median **16,7 ms (60 fps)** |
| **Halaman gerbang: 3x lebih ringan saat diam** | Sembilan tween GSAP tak berujung (6 kartu dek + 3 bola kaca) dipindah ke `@keyframes` CSS, dan animasi di bagian yang di luar layar dijeda IntersectionObserver. Terukur pada build PRODUKSI, 8 dtk tanpa disentuh: kerja utas utama **1,25 → 0,42 dtk**, Script **0,49 → 0,02 dtk**, bingkai median **16,7 ms** (60 fps) baik saat diam maupun saat digulir |
| **Dimensi Kompetisi jadi nyata (OSM)** | 7.505 elemen Overpass -> **3.519 POI usaha** di `business_pois`, mengisi C01, C02, C03, C05, C06, D08, D09 untuk **seluruh 708 heksagon**. Taksonomi 83 tag OSM -> 8 kelas induk di `config.py`. Skor dihitung ulang: **rho 0,9500**, 165 heksagon (23,3%) berpindah kuadran — jauh lebih besar daripada D04 (3%), wajar karena C06 sendirian memegang 45% bobot indeks kompetisi. Kartu gerbang sudah dipotret ulang |
| **Cakupan OSM TIDAK merata antar-kawasan** | Terukur: 6,3 POI/heksagon di Dukuh Atas BNI sampai 0,6 di Harjamukti — sepuluh kali lipat. **312 dari 708 heksagon ber-C01 nol** (97% Harjamukti, 24% Dukuh Atas). Sebagiannya nyata, sebagiannya kerapatan PEMETAAN. Dampaknya ke skor ternyata kecil dan berlawanan arah dugaan (C03 keragaman yang dibalik mengimbangi C06 yang rendah), tetapi insight `SEPI_PESAING` kini dijaga `kepadatan_poi_total > 0` supaya lubang data tidak pernah disodorkan sebagai alasan memilih lokasi |
| **Atribusi ODbL** | OSM dan openrouteservice dipasang lewat `customAttribution` di `PetaInteraktif.tsx`, terpisah dari atribusi MAPID atas ubinnya. `/meta/siap` ikut membawa `atribusi` + `sumber_terbuka`, dan `catatan_data` sekarang DITURUNKAN dari jumlah baris — ia berhenti mengaku "seluruh isi peta sintetis" begitu ada variabel yang tidak lagi sintetis |
| **`demo_seed` tidak bisa lagi menghapus data nyata** | `_pastikan_boleh_menghapus()` menolak jalan kalau basis datanya memuat rute ORS, isochrone, atau POI bukan-demo. Dilewati hanya dengan `--paksa` |
| **Data misi MAPID termuat** | Ditarik ulang 29 Agu 2026: **988 titik** (sebelumnya 866 pada 27 Agu — bertambah 122 dalam dua hari, karena API-nya disaring per POLIGON dan mengembalikan survei SELURUH peserta). **46 jatuh di dalam 708 heksagon**, mengisi 25 heksagon (median jarak seluruh titik ke pusat pilot terdekat: 11,1 km — surveinya tersebar se-Jabodetabek). Mengisi DELAPAN variabel: B06, B07, B08, C07, C08, D10, D12, P03 — dan **IPTT akhirnya bisa dihitung**. rho 0,9744, 56 heksagon (7,9%) pindah kuadran. **20 heksagon `observed`, 688 `predicted`**, seluruhnya tetap RENDAH (maks 4 titik/heksagon, ambang SEDANG 10) | 
| **Pita "Data demo" berhenti bisa hilang terlalu cepat** | Sebelumnya dipicu `observasi_misi == 0`, jadi 27 titik yang mendarat di 20 heksagon akan MEMADAMKANNYA sementara 688 heksagon masih sintetis. Sekarang diturunkan dari `count(data_source != 'observed') > n/2` | 
| **P04 bukan perbandingan jumlah listing** | Ia "sewa tahunan ÷ harga jual" — rasio imbal hasil. Sempat saya daftarkan sebagai bisa-diisi-tanpa-AI; keliru. Menuntut harga, dan harga ada di foto → A1 | 
| **ZoneGuard akhirnya menjaga sesuatu yang nyata (L01, L02, L03)** | Zonasi RDTR ATR/BPN lewat GISTARU — API-nya tidak terdokumentasi, dibongkar dengan menyadap permintaan jaringan portalnya. **328 dari 708 heksagon** berzona (hanya DKI; Kota Depok dan Kota Bekasi tidak terdaftar di GISTARU sama sekali). 269 diizinkan, **2 dilarang**, 437 tidak diketahui. Kedua yang dilarang Zona Ruang Terbuka Hijau di Tanah Abang berisiko banjir ~1,0, dan keduanya kini berskor 0,0. Bonus: kolom `KRB_03` RDTR memberi **L03 sekaligus**, jadi InaRISK tidak diperlukan | rho 0,8841, 41 heksagon (5,8%) pindah kuadran, selisih maks 89 poin. Perpindahannya seluruhnya TEGAK, sesuai harapan: L03 masuk lewat IBR dan L01 menolkan — keduanya sumbu peluang |
| **D05 nyata dari relasi rute OSM** | Variabel berbobot terbesar kedua di seluruh model (0,40 di IPT) berhenti jadi `rng`. 297 relasi -> **144 lin** angkutan umum, 3.538 titik henti berkoordinat, **431 dari 708 heksagon** dilewati setidaknya satu lin. Dua koreksi yang keduanya terukur: OSM memecah satu lin jadi banyak relasi varian ("Lin Lingkar Cikarang" = **14 relasi**), dan `route=train` mencampur 46 lin ANTARKOTA dengan 4 lin KRL Commuter. Sebelum dikoreksi, Stasiun Bekasi berskor 702 melawan Dukuh Atas 259; sesudahnya sepuluh teratas dikuasai Dukuh Atas — simpul transit terbesar Jakarta, sebagaimana mestinya |
| **C04 nyata dari tag `cuisine` OSM** | Ia tidak pernah benar-benar menunggu A4: OSM sudah membawa taksonomi masakannya sendiri, 100 jenis di wilayah studi. Tagnya opsional (41,8% POI kuliner), jadi entropinya DITOLAK untuk heksagon yang tagnya terlalu tipis — **27 dari 708** lolos, sisanya kosong |
| **18 variabel sintetis dikosongkan** | `s7_publish.py --kosongkan`. Nol angka karangan tersisa di `hex_features`, dan `GET /meta/siap` sekarang MENGHITUNG berapa dari 43 yang terisi alih-alih membaca daftar tulis tangan: **25 dari 43, seluruhnya bisa dikutip**. rho 0,5775 terhadap skor lama, 330 heksagon (46,6%) pindah kuadran — perubahan terbesar sejauh ini, dan memang seharusnya begitu |
| **7.186 baris profil jam palsu bertanda `observed` dibuang** | `hex_hourly_profiles` berisi 7.186 baris untuk 474 heksagon yang seluruhnya dibangkitkan `demo_seed` dari struk karangan — dan `sumber_data`-nya tertulis `'observed'`. Ia menggerakkan Commuter Clock, fitur BERBAYAR. Persis kesalahan yang sudah pernah diperbaiki pada badge keyakinan, terulang di tabel lain |
| **Simulasi hidup tanpa sebaris pun data survei** | Sewa dan harga rata-rata per pembeli boleh DIISI SENDIRI, dan yang diisi menang atas angka basis data. Bukan kelonggaran melainkan pengakuan siapa yang memegang angka lebih benar: orang yang menimbang sebuah ruko sudah memegang penawaran pemiliknya, dan harga jual adalah rencananya sendiri. Akibatnya `pembeli impas = sewa / (hari x harga x margin)` bisa dihitung DI MANA PUN - ketiga bahannya milik pengguna. Itu yang jadi judul sekarang kalau laba tidak bisa dihitung. `sumber` ikut dikirim supaya antarmuka membedakan angka yang diketik orang dari angka yang diukur pipeline |
| **Nilai tanah & RDTR luar DKI: dicari sampai habis, buntu** | Diverifikasi 29 Agu 2026. NJOP tidak diterbitkan terbuka oleh satu pun dari tiga penerbitnya (Jakarta Satu `Bapenda`/`BPRD` 499, `BPN/Persil` terbuka tetapi tanpa kolom nilai, Bhumi ATR/BPN cuma bidang tanah, nol folder ZNT di 106 folder GISTARU). Kota Depok dan Kota Bekasi terkonfirmasi tanpa RDTR lewat indeks KEDUA — `_3200_RDTR_PROVINSI_JAWA_BARAT_PR_PERDA` memuat 34 layer yang sudah diperdakan, seluruh entri "Bekasi" adalah Cikarang. Jadi L01–L03 berhenti di 328/708 karena batas SUMBERNYA |
| **Rencana survei 30 heksagon** | `docs/data.md` bagian 11. Diturunkan dari basis data, bukan ditulis tangan: `n_titik_misi = 0` + kuadran rekomendasi + ada POI = 260 kandidat, lima teratas per kawasan menurut `opportunity_score`, berkoordinat. Yang membuatnya mendesak satu angka: **28 dari 30 heksagon berskor tertinggi belum pernah disurvei** — justru yang direkomendasikan produk dan yang akan diklik juri |
| **Kunci MAPID dicabut dari peramban** | Diverifikasi di build produksi: nol berkas di `dist/` memuat kunci, nol URL yang diminta peramban membawa `key=` atau `access_token=`. Gaya basemap jadi empat berkas statis di `frontend/public/basemap/` (224 KB), dibangkitkan `scripts/gaya-basemap.mjs` lewat `GET /meta/basemap/{gaya}/style.json` di backend. Ubin, font, dan sprite tetap diambil peramban LANGSUNG dari MAPID |
| **GapFill (s5_impute) lengkap dan teruji** | Random Forest + spatial k-fold PER KAWASAN, melaporkan R2 dan MAE apa adanya beserta pembanding "menebak rata-rata". 34 uji di `test_s5_impute.py`. **MENOLAK jalan di basis data hari ini** dan itu penjaganya bekerja: D10 dan B07 masing-masing terisi di 8 dari 708 heksagon, jauh di bawah `MIN_GROUND_TRUTH = 30` |
| **Audit PRD di peramban** | `frontend/scripts/audit-prd.mjs` - 43 asersi menelusuri keenam acceptance criteria PRD di Chromium sungguhan, dua lintasan (tamu dan pelanggan premium), plus pemeriksaan kepatuhan kunci dan basemap. **43/43 lolos di build PRODUKSI** (`vite preview` :4173, backend hidup). Empat menjaga bagian `#temuan` dan empat menjaga sumbu prestise; keduanya tidak bisa hidup di uji unit mana pun, karena yang ditanyakan adalah apakah angka yang TERLIHAT sama dengan angka yang dikirim |
| **Terbitan publik HIDUP** | Diterbitkan 3 Sep 2026. Backend `https://loconomics-api.azurewebsites.net` (App Service Linux, Python 3.11, region **Indonesia Central** — bukan Southeast Asia, lihat baris jebakannya). Tier bawaannya **F1** (gratis), **dinaikkan sementara ke B1 + Always On untuk pameran 4–6 Sep 2026** dan **wajib diturunkan lagi 6 Sep pukul 00.00** — ditagih per JAM ($0,017/jam Linux di region ini, jadi dua hari ≈ $0,80 dari kredit $100). Urutan menurunkannya tidak boleh dibalik, karena F1 tidak mendukung Always On dan penurunan tier akan ditolak selama fitur itu menyala: `az webapp config set --name loconomics-api --resource-group loconomics-rg --always-on false` DULU, baru `az appservice plan update --name loconomics-plan --resource-group loconomics-rg --sku F1`. Alasan menaikkannya bukan kecepatan melainkan kuota: F1 berkuota 60 menit-CPU per hari, dan kalau habis aplikasinya menjawab 403 sampai tengah malam UTC — kegagalan yang tidak bisa dipercepat siapa pun, tepat di depan juri. Frontend terbit ke **DUA** tempat dari repo yang sama: **`https://loconomics.pages.dev`** (Cloudflare Pages, yang dipakai untuk juri — tanpa nama pribadi di URL) dan `https://fijarsatria.github.io/loconomics/` (GitHub Pages, cadangan). Keduanya diizinkan `CORS_ORIGINS`, jadi berpindah di antaranya nol waktu mati. Bedanya terukur: di Cloudflare `_headers` dan `_redirects` **berlaku** — empat header keamanan dan tiga aturan cache per folder benar-benar ada di respons, sesuatu yang tidak pernah sekali pun hidup di GitHub Pages. Ketiga pemeriksaan hidup di alurnya lolos, dan diulang sendiri dari luar: `/meta/siap` menjawab **708 heksagon, 708 skor, 12.744 profil jam, 6 kawasan, 43/43 variabel terisi**, revisi migrasi `a7f2c9d41b83`, `lingkungan: produksi`. Penjagaan berbayar diuji dari luar sebagai TAMU dan menahan: `variabel`/`faktor` kosong dengan `terkunci`, `/pricelens` dan `/commuter-clock` menjawab 401. **Kedua** berkas worker MapLibre terbit 200 (jebakan #1), dan URL backend terbukti ter-*bake* ke dalam bundel (jebakan #11) — bukan cuma halaman depannya yang menjawab. Satu panggilan AI sungguhan ke produksi: **18,7 detik, 6 panggilan alat backend + 2 aksi peta**, lewat `gemini-3-flash-preview`. **Yang harus diingat**: F1 tidak punya Always On DAN berkuota **60 menit-CPU per hari** — kalau habis, aplikasinya menjawab 403 sampai tengah malam UTC. Untuk hari pameran atau minggu penjurian, naikkan ke B1 (`az appservice plan update --name loconomics-plan --resource-group loconomics-rg --sku B1`, lalu `az webapp config set --always-on true`) |
| **Berkas deployment** | **Backend ke Azure App Service** lewat `.github/workflows/backend-azure.yml` — Azure for Students memberi $100/12 bulan **tanpa kartu**, dan Render menuntut kartu. Alurnya menguji dulu (`test_infra` + `test_ai_loop`, keduanya sudah diuji lolos tanpa `.env`), menerbitkan, lalu **memeriksa terbitan yang baru naik dari luar**: `/health`, header CORS untuk asal frontend yang sebenarnya, dan satu tiket karangan `Bearer a.b.c` ke `/akun/saya` yang harus dijawab 401 — 500 berarti `AUTH_SECRET` belum disetel. Ketiga pemeriksaan itu diuji ke DUA arah: lolos di terbitan sehat, dan gagal dengan pesan yang bisa ditindaklanjuti di terbitan yang rusak. `render.yaml` dipertahankan sebagai cadangan yang sudah dibetulkan dan teruji. Plus `frontend/public/_headers` dan `_redirects` - keduanya format Cloudflare Pages, dan sejak 4 Sep 2026 **akhirnya berlaku**, karena frontend ikut terbit ke Cloudflare Pages. Di GitHub Pages keduanya tetap diabaikan. Berkas yang sama, dua terbitan, dua nasib - jadi "berkasnya ada" tetap bukan bukti apa pun, dan yang membuktikan tetap `curl -I` ke terbitan yang MANA |
| **Pengerasan keamanan (13 Sep 2026)** | Diaudit, lalu diperbaiki, lalu tiap perbaikan **dibuktikan merah lewat sabotase** sebelum dinyatakan hijau. (1) **Header keamanan di SETIAP respons API** — `main.py::header_keamanan`, middleware dan bukan `Depends`, supaya respons GALAT ikut membawanya; kelimanya terukur ada pada 200 maupun 404. HSTS hanya kalau `settings.produksi` (di localhost ia mengunci peramban pengembang ke https berbulan-bulan). (2) **`/openapi.json` ditutup di produksi.** `/docs` dan `/redoc` sudah lama tertutup, tetapi skemanya masih 200 — diukur pada terbitan yang hidup — jadi siapa pun bisa menempelkannya ke editor.swagger.io dan mendapatkan tombol Try it out yang sama ke API yang sama. Yang tersembunyi cuma halamannya. (3) **`AUTH_SECRET` wajib ≥ 32 karakter di produksi**, bukan cuma tidak kosong: tiket ditandatangani HMAC-SHA256, jadi kunci pendek dicari OFFLINE dari satu tiket sah yang setiap pengguna pegang di localStorage — tanpa menyentuh server, tanpa satu pun log. (4) **Pembatas laju untuk ketiga endpoint PDF** (`penjaga_berat`), dan yang dijaga bukan uang melainkan **jatah 60 menit-CPU per hari** F1: satu PDF memakan 1–3 detik CPU, dan begitu jatah habis SELURUH aplikasi menjawab 403 sampai tengah malam UTC. Embernya **per akun**, bukan per IP — juri bisa membuka situs ini dari satu jaringan yang sama. Dipasang lewat `dependencies=[...]` di dekorator, jadi pemanggil Python langsung (uji, alat AI) tidak ikut terkena. (5) **CSP untuk KEDUA terbitan** — lihat `arsitektur.md`; disuntikkan saat build dari `_headers`, diuji di peramban lebih dulu (nol pelanggaran di `terang` dan `satelit`). Yang diaudit dan **tidak** perlu diperbaiki, dicatat supaya tidak diaudit ulang: nol SQL dinamis dari masukan pengguna di `app/` (yang ada cuma nama kolom internal di dua skrip pipeline); `baca_tiket` sudah `hmac.compare_digest` + cek `exp` dan tidak pernah membaca `alg` dari kepala tiket; formulir masuk sudah kebal enumerasi DAN kebal beda waktu (`_SIDIK_HANTU`); argumen `pengguna` dari model selalu dibuang sebelum alat dipanggil; nol berkas `.env` terlacak git dan **nol kunci di seluruh 73 commit**; `npm audit --omit=dev` nol; backend tidak menyajikan berkas statis dan `python-multipart` tidak terpasang sama sekali, jadi CVE multipart Starlette tidak bisa dijangkau; `h11` 0.16.0 sudah di atas CVE-2025-43859. **Satu temuan yang sengaja TIDAK ditindak**: `playwright` 1.49.1 terkena GHSA-7mvr-c777-76hp (mengunduh peramban tanpa memverifikasi sertifikat). Ia dependensi PENGEMBANGAN yang tidak pernah ikut ter-deploy, jalur rentannya hanya `npx playwright install`, dan perambannya sudah terpasang — sementara naik ke 1.55.1 menuntut unduhan peramban baru dan berisiko merusak satu-satunya alat verifikasi ini beberapa hari sebelum pengumpulan. Perbaikannya `npm i -D playwright@latest`, dijalankan SESUDAH lomba |
| **Dua temuan dari pemeriksaan terbitan hidup (13 Sep 2026)** | Ditemukan dengan MEMBUKA situsnya, bukan dengan membaca kodenya - keduanya lolos setiap uji yang ada. (1) **Terbitan Cloudflare naik tanpa kunci basemap.** `loconomics.pages.dev` - justru URL yang dipakai juri - mengirim setiap permintaan ubin TANPA `?key=`, dan MAPID menolak yang tanpa kunci sejak 6 Sep. Yang tampil peta PUTIH dengan label mengambang. Sebabnya: `pages.yml` mengoper secret ke build GitHub Pages, sementara Cloudflare Pages membangun sendiri dari dasbornya dan variabelnya tidak pernah diisi di sana. **Tindakan pemilik repo**: isi `VITE_MAPID_BASEMAP_KEY` di Cloudflare Pages > Settings > Environment variables, lalu jalankan ulang deployment-nya. Sejak sekarang build rilis tanpa kunci GAGAL KERAS (`vite.config.ts::kunciBasemapWajib`) alih-alih naik diam-diam. (2) **Hambatan 20-permintaan-per-menit Gemini diperlakukan sebagai pemadaman 15 menit** dan dikabarkan sebagai "jatah harian habis". Balasan 429-nya membawa `retryDelay` sendiri - dua detik - dan kini dibaca: jendela penuhnya mengikuti yang diminta penyedianya (lantai 30 detik, atap 15 menit), tidur antar-percobaan mengikuti angka itu juga, dan kalimat ke pengguna berbunyi "membatasi jumlah pertanyaan per menit, coba lagi sekitar 59 detik lagi". Diverifikasi pada balasan 429 yang sungguhan. **Yang perlu diketahui untuk hari penjurian**: satu pertanyaan pengguna = 6-8 panggilan model (loopnya agentik), sementara jatah gratis Gemini 20 panggilan per MENIT - jadi dua orang bertanya bersamaan sudah bisa membentur batasnya. Kalau penjurian berlangsung serentak, nyalakan billing pada kunci Gemini untuk menaikkan batas itu; tanpa itu yang terjadi bukan kegagalan melainkan antrean berkalimat jujur |
| **Pusat Harjamukti diperbaiki** | Dari `(-6,3706 . 106,8556)` ke `(-6,37389 . 106,89567)` — OSM node/6720467138, `network=LRT Jabodebek`. Pergeseran **4.443 m**. Keenam pusat diverifikasi ulang ke OSM; lima lainnya meleset 33–328 m dan dibiarkan. 127 heksagon dibangun ulang di tempat yang benar, seluruh data turunannya ditarik ulang. Bukti perbaikannya satu angka: penarikan OSM di sana sekarang menemukan **Stasiun LRT Harjamukti pada 0 m**, sebelumnya nol stasiun rel |
| **`PUSAT` jadi satu sumber kebenaran** | Dulu tiga salinan yang saling cocok dan sama-sama salah. Sekarang `pipeline/config.py::PUSAT`, diimpor `s1_ingest.py` dan `demo_seed.py`. Frontend dijaga tiga uji baru di `test_aturan.py`, salah satunya membandingkan dengan koordinat stasiun OSM — bukan dengan salinan lain |
| **Bagian Sumber Data & Batasan di gerbang** | B.5 menuntut "Metodologi dan Sumber Data"; sebelumnya isinya hanya ada di `docs/`, tidak di WebGIS-nya. Sekarang bagian `#sumber`: tujuh sumber dengan lisensi, variabel yang diisinya, dan **cakupan per heksagon** - plus batasannya. Seluruh angkanya dibangkitkan `s7_publish.py --ekspor`, jadi ia tidak bisa basi tanpa ketahuan. Ini juga satu-satunya bagian yang tetap hidup di terbitan publik tanpa backend |
| **Bagian Temuan di gerbang** | B.5 menuntut "Analisis dan Insight", dan yang paling kuat di proyek ini justru tidak pernah sampai ke mata juri — ia hidup di `CLAUDE.md`. Sekarang bagian `#temuan`: **empat kali pengukuran membantah dugaan yang wajar**, tiap satu berbentuk *dugaan → ukuran → yang berubah di produk*, dengan grafik batangnya sendiri. Rute jalan kaki memutar **1,78×** rata-rata (median 1,53×, terjauh 7,07×; 161 heksagon ≥ 2×). Stasiun Manggarai justru berjangkauan **tersempit** — 0,96 km² melawan 3,00 km² Dukuh Atas, emplasemen relnya memotong ke segala arah. OSM memetakan Dukuh Atas **16×** lebih rapat daripada Harjamukti, dan **318 dari 708** heksagon nol usaha terpetakan. **13 heksagon** dinolkan zonasinya, **417** sengaja tidak dinilai. SELURUHNYA — termasuk kalimatnya — dirangkai `s7_publish.hitung_temuan()` dari basis data; komponennya `GerbangTemuan.tsx` tidak memuat satu pun angka. Temuan yang bahannya tidak ada TIDAK diterbitkan, jadi tabel kosong memperpendek daftarnya alih-alih mengarang |
| **Sumbu prestise berhenti mengaku diukur dari tampilan** | Sumbu datar Kompas Kuadran - SETENGAH tesis produk ini - dirata-ratakan `hitung_prestise_visual()` dari LIMA bahan dengan `skipna=True`. Terukur 2 Sep 2026: **M03 (kesan mewah dari foto) 0/708 dan P02 (posisi NJOP) 0/708**, dan keduanya justru satu-satunya yang menilai tampilan secara LANGSUNG. Yang menggerakkan sumbunya tinggal C05 porsi waralaba (r +0,85; terisi 390/708), M01 kerapatan bangunan (r +0,66; 708/708), dan M02 luas bangunan (r +0,24; 699/708). **390 heksagon berdiri di atas tiga bahan, 309 di atas dua, sembilan di atas SATU** - sementara panel menulis "Bangunan dan tokonya **terlihat** lebih mahal", kalimat yang mengaku ada yang melihat. Sekarang: kalimatnya berbunyi "Diperkirakan tampak…", dan di bawahnya `frasaPrestise()` menyebutkan sumbu itu berdiri di atas bahan apa saja - **dibangkitkan dari `cakupan_prestise` milik backend**, jadi ia berubah sendiri begitu satu bahan terisi dan hilang sendiri begitu kelimanya terisi. Muncul di DUA tempat: panel detail (per heksagon) dan "Cara membacanya" di diagram penuh (ikut tersaring kawasan). Yang TIDAK dilakukan: menyembunyikan sumbunya - ia menentukan kuadran, dan kuadrannya sudah tergambar di peta. Ambang `AMBANG_INDEKS_LAYAK_TAMPIL` juga tidak dipakai, karena 3 dari 5 = 60% lolos ambang apa pun justru pada keadaan yang jadi masalahnya. Kedua sumbu diperiksa dan **belum runtuh**: Pearson +0,083, Spearman +0,133, jauh di bawah 0,8 yang disebut `produk.md` sendiri |
| **Atribusi lengkap** | RDTR ATR/BPN (GISTARU) sempat mengisi L01-L03 tanpa pernah disebut. Sekarang ada di kontrol peta, di `#sumber`, dan di `/meta/siap` - dan daftar atribusi backend disaring PER ENTRI menurut kolom yang membuktikan sumbernya termuat, bukan satu syarat untuk seluruh daftar |
| Dokumentasi | 7 dokumen di `docs/` |

**523 asersi otomatis lolos** di sembilan berkas uji — 304 di `backend/tests/` (44+57+53+36+114), 219 di `pipeline/` (130+43+14+32). Ditambah **43 asersi audit peramban** di `frontend/scripts/audit-prd.mjs`, yang menuntut backend dan frontend hidup sehingga sengaja TIDAK dijumlahkan ke angka di atas.

Sembilan asersi terakhir yang masuk menjaga `render.yaml`, dan alasannya layak dicatat: berkas itu memuat tiga kerusakan yang membuat backend gagal naik atau gagal melayani, dan **tidak satu pun dari 497 asersi sebelumnya bisa menangkapnya** — semuanya berjalan dengan `.env` lokal, satu-satunya lingkungan yang bentuknya kebetulan benar. Berkas konfigurasi yang belum pernah dieksekusi adalah kode yang belum pernah dikompilasi.

> **Ukur di build PRODUKSI, bukan di dev server.** Dev build membawa React mode
> DEV (`jsxDEV`, StrictMode render ganda), sumber tanpa minifikasi, dan HMR.
> Selisihnya besar dan menyesatkan: bingkai median halaman gerbang **100 ms di
> dev, 16,7 ms di produksi** — sama persis kodenya. `npx vite build && npx vite
> preview` lalu ukur di `:4173`, yang sudah ada di `cors_origins`.

### Perombakan 11 September 2026 — heksagon sungguhan, panel AI, dan tipografi

| Bagian | Bukti |
|---|---|
| **Kartu Solusi memakai heksagon SUNGGUHAN kartu itu** | Kisi heksagon karangan yang menutupi kotak gambar dicabut. Yang menggantikannya datang dari `KARTU_GERBANG[i].sorot`, dibangkitkan `scripts/potret-kartu.mjs --sorot`: pusat tiap heksagon dalam piksel kotak gambar, dihitung dengan kamera `fitBounds` yang SAMA dengan yang menggambar WebP-nya. Diuji — kamera aritmetika di skrip dan `map.project` MapLibre berselisih **0,0000 piksel**. Bentuknya simpangan enam simpul yang dirata-ratakan; simpangan antar sel terukur **0,01 piksel**. Warnanya DIBACA KEMBALI dari kanvas MapLibre yang menggambar heksagon memakai `WARNA_LAYER` milik aplikasi — nol tabel warna kedua. Dan yang disorot menjawab pertanyaan kartunya: skor tertinggi untuk "memilih lokasi", sewa termurah untuk "menakar sewa", zona terlarang untuk "memastikan boleh" |
| **`--sorot` bisa dijalankan tanpa ubin MAPID** | Menggambar WebP menuntut ubin MAPID hidup; menghitung geometri heksagon tidak menuntut apa pun selain basis data. Selama ubin MAPID menolak (401, hari ketujuh), mode penuh akan menerbitkan enam kartu tanpa basemap — mode ini tetap jalan dan gambarnya tidak disentuh |
| **Layer Hidden Gem akhirnya berwarna** | `WARNA_LAYER.hidden_gem` memakai `var(--q-gem-lembut)` di dalam ekspresi cat. Kanvas WebGL tidak mengenal variabel CSS: SELURUH layer isiannya gagal dipasang, tanpa galat, di peta maupun di kartu gerbang. Ditemukan dengan membaca piksel kartunya kembali. Diperbaiki lewat `lembutPeta` — pasangan hex-harfiah dari `lembut`, persis seperti `warnaPeta` berpasangan dengan `warna` |
| **Bagian Latar belakang: tiga masalah, satu per satu** | Sebelumnya tiga baris daftar bergaris rambut dengan SATU gambar untuk ketiganya. Sekarang tiap masalah punya barisnya sendiri, gambarnya sendiri, dan gilirannya sendiri saat digulir — berselang kiri-kanan, jadi arah masuknya yang memberi tahu bahwa ini masalah berikutnya. Bahasa visualnya sengaja BEDA dari Ekosistem: bukan tulang punggung + foto dalam bingkai membulat, melainkan **pelat alat ukur** dengan siku di keempat sudut, kisi milimeter, dan label huruf mono. Ketiga gambarnya menggambarkan kalimatnya, bukan menghiasinya — titik data yang padam di dinding, garis sewa yang mengikuti tampilan alih-alih penjualan, dan satu heksagon yang berubah jadi Hidden Gem saat sapuan melewatinya. **Nol angka** di ketiganya, disengaja: angka sungguhan ada di enam kartu bagian berikutnya |
| **Jarak sengaja antara hero dan Latar belakang** | 58–68vh berisi satu tali yang TUMBUH mengikuti gulir dan satu heksagon di ujungnya. Hero-nya dipatok dan menyusut di baliknya; tanpa jeda ini judul bagian berikutnya sudah menutupinya sebelum gerakan itu sempat terbaca |
| **Tipografi gerbang: satu sumbu, bukan dua** | `.papan` (lebar 125%) dipakai di TUJUH tingkat judul sekaligus — nama produk, judul bagian, judul kartu, judul baris, judul panel. Pada judul kartu setinggi 16px ia bukan penekanan melainkan LEBAR HURUF yang berbeda dari kalimat di sebelahnya; dilaporkan pemilik repo sebagai "font di landing page kayak beda sekali". Sekarang `.papan` hanya identitas (papan nama bilah, LOCONOMICS hero, nama raksasa penutup), `.judul-bagian` untuk judul bagian, `.judul-anak` untuk judul kartu/baris. Diverifikasi dengan membaca `font-stretch` seluruh judul di halaman: 3 pada 125%, sisanya 100% |
| **Panel AI: bilah judul dan lencana status dicabut** | Bilah itu memuat nama panel yang sudah tertulis di tab tepat di atasnya, dan mengkliknya membawa orang kembali ke daftar lokasi — persis yang dilakukan tab di sebelahnya. Lencana "Siap / Memeriksa" ikut pergi: ia memberi kabar tentang mesinnya, bukan tentang pertanyaan orang yang sedang mengetik. Asersi audit yang mencari kata "siap" diganti asersi yang lebih tegas: kotak tanyanya HARUS bisa diketik kalau asisten hidup, dan panel HARUS mengatakan sebabnya kalau mati |
| **Riwayat percakapan AI** | Disimpan di peramban orang yang memakainya, bukan di server — isi percakapan bisa memuat lokasi incaran seseorang, dan tidak ada alasan hal itu perlu meninggalkan mesinnya. Backend memang sudah tanpa-status. Dibatasi 20 percakapan; laci menutupi percakapan alih-alih menggesernya, dan dibaca ulang dari localStorage tiap kali dibuka — jadi percakapan dari TAB LAIN ikut muncul |
| **Pintasan lokasi di jawaban AI** | `hex_disebut` sudah dibawa tiap jawaban sejak awal; yang belum ada cara menekannya. Tiap heksagon yang disebut jadi tombol yang menerbangkan peta ke sana, dengan label yang diambil dari `/hex/{h3}` (bagian gratis: skor, kuadran, kawasan). Skornya yang berdiri di depan, bukan nama kawasannya — jawaban AI hampir selalu menyebut beberapa heksagon di SATU kawasan, jadi delapan baris "Manggarai" tidak membantu siapa pun memilih. Tabnya TIDAK diganti saat ditekan: melempar orang ke daftar lokasi berarti menutup percakapan yang barusan menyebut lokasi itu |
| **Layar pembuka panel AI** | Papan nama di TENGAH-TENGAH panel (sebelumnya di tengah atas, karena tingginya cuma setinggi isinya), 34px, dengan tiga lapis cahaya di belakangnya: halo yang bernapas, cincin conic yang berputar 22 detik sekali, dan satu sapuan spekular yang melintasi namanya tiap tujuh detik |
| **Dialog Masuk: lambang dan kolom isian** | Heksagon berhuruf "L" diganti papan nama "Loconomics" yang SAMA dengan di bilah pencarian — komponennya sendiri, bukan tiruannya, jadi perilaku per-hurufnya ikut apa adanya. Dan kolom nama pengguna/sandi berhenti jadi BILAH PUTIH di atas gerbang yang hitam: dialognya di-portal ke `<body>`, jadi ia tidak mewarisi palet `.gerbang`, dan selama basemap peta di belakang kebetulan terang `bg-surface` jadi putih. `<body>` sekarang ikut menyatakan `peta-gelap` selama gerbang terbuka |
| **Transisi pulang: peta → halaman perkenalan** | Jalan masuk sudah punya layar pembukanya sendiri; jalan pulang mengganti seluruh layar dalam satu bingkai, dan yang terbaca bukan "kembali" melainkan aplikasi yang mendadak hilang. Sekarang heksagon MEKAR dari tengah sampai menutupi layar (430 ms), lalu memudar di atas halaman perkenalan yang animasi masuknya sudah berjalan di baliknya (430 ms). Dihormati `prefers-reduced-motion` |
| **Halaman gerbang jadi LEBIH RINGAN daripada sebelum perombakan** | Diukur di build produksi dengan gulir 55 px/bingkai — bukan lompatan. Bingkai median **43,2 → 30,6 ms**, dan itu sudah TERMASUK enam lapisan heksagon kartu dan tiga pelat beranimasi yang baru. Tiga sebabnya, ketiganya terukur satu per satu: latar hias hero yang `sticky` terus dilukis di belakang lembar penutupnya sepanjang halaman (11 ms), hanyut parallax pada pelat berlatar gradien bergaris (8 ms), dan `scale` pada `<path>` berisi belasan heksagon (3 ms). Sebelum perombakan, tanpa satu pun tambahan itu: 38,8 ms |
| **Halaman berhenti bisa digeser mendatar di 1024 px** | Keadaan parkir animasi masuk (`x: ±50`) berdiri di luar barisnya, dan tepat di 1024 px dua kolomnya mengisi lebar tanpa sisa: `scrollWidth` 1054 pada `clientWidth` 1024. Diukur di 390/768/1024/1440 — hanya 1024 yang merah, dan penjaga lama hanya menjaga 390. Diperbaiki `overflow-x: clip` pada baris masalah dan baris ekosistem |

---

### Perombakan 11 September 2026, putaran kedua — tema, tingkat berbayar, laporan

| Bagian | Bukti |
|---|---|
| **Sakelar terang/gelap berdiri sendiri** | Sampai hari ini terangnya chrome diturunkan dari gaya basemap. Sekarang tema punya sakelarnya sendiri, bawaannya GELAP, disimpan di peramban; basemap ikut saat temanya DIGANTI tapi tidak saat dimuat, jadi pilihan basemap tersimpan tidak pernah ditimpa. Tirainya lingkaran yang mekar dari tombolnya sampai menutupi layar - bukan `startViewTransition`, karena aplikasi ini berisi kanvas WebGL seukuran layar dan memotretnya dua kali untuk pergantian warna tidak sepadan |
| **Palet TERANG halaman gerbang** | Bukan pembalikan mekanis: aksen teal dan ungu memakai versi tuanya supaya lolos kontras di atas kertas; aurora, sarang, dan lensa yang dirancang sebagai CAHAYA dibalik jadi bayangan; tombol utama yang putih jadi hijau-tua pekat. Bagian TIM tetap gelap di kedua tema. Tiga tempat yang masih dirancang untuk halaman hitam ikut dibalik ARAHNYA: foto Ekosistem, kabut di atasnya, dan ambang menuju jurang (46vh → 15vh, kalau tidak seluruh kaki halaman tenggelam) |
| **Empat indeks dan penjelasan kuadran jadi berbayar** | Dijaga DI SERVER: `indeks.ipt/iae/ikp/ibr` dan `kuadran_penjelasan` benar-benar `None` untuk yang belum membayar, bukan dikirim lalu diburamkan. Yang tetap gratis `cakupan` dan `cakupan_prestise` - keduanya pengakuan bahwa datanya tipis, dan pengakuan tidak boleh jadi barang dagangan. Asersi audit yang mencari kalimat prestise di layar TAMU dipindah ke lintasan pelanggan; di tempatnya berdiri asersi kebalikannya. `test_akun` naik 44 → 48 |
| **Laporan Kelayakan PDF berisi gambar** | Keempat indeks sebagai batang berwarna menurut ARAHNYA (yang "rendah lebih baik" diberi warna berbeda supaya batang panjang tidak otomatis terbaca sebagai kabar baik), petak kuadran 2×2 dengan titik di kotak yang benar, delapan faktor teratas sebagai batang persentil, dan 18 jam operasional sebagai grafik batang. Ditambah blok profil pemiliknya di halaman pertama - nama, rencana usaha, kawasan incaran, dan anggaran yang DIBANDINGKAN langsung dengan sewa lokasi itu |
| **Komparasi: enam aspek baru** | Keramaian sore, penduduk sekitar, keramaian usaha, keragaman usaha, pergantian usaha, sewa per bulan. Semuanya sudah ada di `hex_features` dan sudah dipakai menghitung skor; yang belum ada cuma jalannya ke tabel komparasi. Arahnya ditulis di `ARAH_METRIK` backend - "churn tinggi itu buruk" adalah aturan produk, dan aturan yang hidup di dua tempat cepat atau lambat berselisih. PDF-nya ikut, plus batang skor berdampingan di atas tabelnya |
| **Daftar lokasi dirapikan dan akhirnya dua bahasa** | Dua paragraf kepala dicabut (pertanyaan layer yang sudah tertulis di menu Layer, dan tiga baris yang membedakan dua tab yang sudah berjudul berbeda). Empat pil zona jadi SATU dropdown. Seluruh kalimatnya punya cabang Inggris - "Skor tertinggi dulu" berdiri di antara dua tombol berbahasa Inggris sampai hari ini |
| **Layar tunggu bernama** | Papan nama yang berombak menggantikan batang berkilau di daftar lokasi, rekomendasi, dan komparasi. Batang berkilau menyatakan "sesuatu sedang dimuat"; nama yang bergerak menyatakan SIAPA yang mengerjakannya - dan di produk yang seluruh isinya dihitung sendiri, itu kabar yang berbeda |
| **Halaman gerbang jadi DUA KALI lebih ringan** | Bingkai median di build produksi, gulir 55 px/bingkai: **43,2 → 21,7 ms**. Sebabnya bukan animasi melainkan pelukisan - latar hias hero yang `sticky` terus dilukis di belakang lembar penutupnya sepanjang halaman (11 ms/bingkai untuk gambar yang tidak terlihat siapa pun) |
| **Grafik jam simulasi akhirnya menggambar batangnya** | Ia tidak pernah sekali pun menggambar satu batang: `items-end` membuat kolomnya tidak diregangkan, jadi `height: N%` di dalamnya resolve terhadap nol. Terukur di DOM. Slide-nya sekalian diisi "Ramainya terbagi begini" - empat bagian hari yang dirata-ratakan dari `profil_jam` yang sudah ada di respons yang sama |
| **Lokasi tersimpan bisa dibuka di tempat** | Barisnya membuka rinciannya alih-alih melompat ke peta - daftar ini dibuka DI ATAS peta sebagai dialog, jadi "buka di peta" berarti menutup seluruh daftar untuk melihat satu lokasi. Lompat ke peta jadi tombol DI DALAM rinciannya. Dinamika kawasan akhirnya menyebut gunanya |
| **Isian heksagon turun ~25% lagi** | Permintaan kelima dengan sebab yang sama: jalan dan blok bangunan tertelan isian. Garisnya naik 1,6 → 1,7 px sebagai gantinya - tukaran yang sama dengan turunan keempat, dan satu-satunya alasan isian setipis ini masih membentuk petak |
| **Seluruh antarmuka peta akhirnya dua bahasa** | Yang tersisa empat panel besar - `PanelInsight`, `Simulasi`, `Premium`, `Rekomendasi` - plus kosakata bersama yang dipakai ketiganya: 43 nama variabel (`ARTI_VARIABEL_EN`), keempat indeks berikut pertanyaan dan kata-katanya, nama & keterangan kuadran, pertanyaan tiap layer, lencana keyakinan, dan enam belas jenis usaha. Ditutup dengan sapuan statis atas seluruh `src/` dan potret Playwright di build produksi berbahasa Inggris: yang tersisa di layar cuma kalimat yang dirakit backend |
| **Angka ikut berpindah bahasa** | "Rp1.827" dibaca orang Inggris sebagai satu koma delapan - selisih seribu kali pada angka yang dipakai menimbang sewa. `lib/format.ts` membaca `documentElement.lang`, jadi separator dan singkatannya ("jt"/"M" vs "M"/"B") ikut sakelarnya tanpa satu pun komponen perlu meneruskan bahasa |
| **Penjaga lintas berkas DIPERKUAT, bukan dilonggarkan** | `ARTI_VARIABEL_EN` berdiri terpisah justru supaya `test_arti_variabel_lengkap_dan_sama_dengan_frontend` tetap bisa mencocokkan baris `ARTI_VARIABEL` huruf per huruf antara Python dan TypeScript - dan ujinya sekarang menuntut 43 nama Inggris juga. Dibuktikan dengan mencabut satu entri lalu memastikan ia berteriak |
| **Responsif diukur, bukan diperiksa sekilas** | Nol gulir mendatar di 390/768/1024/1440 pada build produksi, dan itu menemukan dua bug: keadaan parkir animasi masuk melebarkan halaman TEPAT di 1024 px (penjaga lama hanya menjaga 390), dan tumpukan tombol kiri peta duduk di atas lembar bawah di 390 karena dua elemen memakai persen dari induk yang berbeda |

---

### Perombakan 11 September 2026, putaran ketiga — kalimat backend, sakelar tema, mode terang

| Bagian | Bukti |
|---|---|
| **Kalimat yang dirakit backend akhirnya ikut berbahasa** | Belasan kalimat, seluruhnya dari server: penjelasan kuadran, penjelasan ZoneGuard, label RiskRadar, catatan Commuter Clock dan konteks simpul, sepuluh peringatan simulasi, sembilan alasan rekomendasi berikut ringkasan dan catatannya, alasan GemFinder, catatan riwayat, catatan dinamika. Semuanya pindah ke satu katalog berkunci `KALIMAT` di `core/aturan.py` — percabangan yang MEMILIH kalimat tetap di tempatnya, yang pindah cuma teksnya. `bahasa` disalurkan sebagai parameter endpoint, bukan header: parameter ikut jadi kunci `@ber_cache` dengan sendirinya, header tidak. Dibuktikan hidup lewat `curl` di kedua bahasa untuk detail, simulasi, riwayat, dinamika, dan rekomendasi |
| **Uji menuntut PLACEHOLDER-nya sama, bukan cuma terjemahannya ada** | Cabang Inggris yang lupa membawa `{sewa}` tidak akan pernah berteriak sendiri — ia cuma menghilangkan angkanya dari kalimat, dan kalimat tanpa angka persis yang dilarang seluruh `aturan.py`. Ujinya membandingkan himpunan medan `str.Formatter` kedua cabang tiap kunci, plus menuntut kelima kamus `_EN` berkunci sama dengan kembarannya. `test_aturan` 55 → 57 |
| **Rupiah ikut pemisah ribuan bahasanya** | "Rp1.827" dibaca pembaca Inggris sebagai satu koma delapan. `rp()` di backend dan `lib/format.ts` di frontend sekarang sepakat: titik untuk Indonesia, koma untuk Inggris |
| **Setelan "Nama tempat: Mati" akhirnya benar-benar mematikan** | `KERAPATAN_NAMA[k]?.geser ?? 0` memakan `null` yang justru satu-satunya nilai berarti khusus di kamus itu, jadi "Mati" berperilaku persis seperti "Normal" sejak setelan ini dipasang. Dilaporkan dua kali |
| **Sakelar tema jadi pil kaca, dan ikonnya berhenti terbalik** | Bentuk baru sesuai dua gambar rujukan pemilik repo: kenop meluncur, matahari kiri saat terang, bulan kanan saat gelap, dengan katanya di sisi yang tersisa. Ikonnya sekarang menyatakan KEADAAN, bukan tujuan — begitu ada kata di sebelahnya, "Dark" di sebelah matahari jadi kalimat yang bertengkar dengan dirinya sendiri. Tempatnya ikut pindah: tengah bawah hero di gerbang, dan ke dalam menu pengaturan di peta |
| **Tirai pergantian tema dihaluskan** | Mekarnya membawa opacity-nya sendiri (0,55 → 1) alih-alih pekat sejak piksel pertama, berhenti di skala 1,04 supaya sudut layar tidak pernah tertinggal, dan membukanya ikut mengembang sambil hilang — searah dengan gerakan yang barusan dilihat mata, jadi terbaca sebagai satu tarikan napas |
| **Mode terang berhenti rusak di tiga tempat** | Scrim kartu Solusi (setengah gradiennya dipatok warna gelap), ambang menuju bagian TIM (masker `sticky` yang jadi lubang putih permanen), dan bilah atas yang tetap berbahan terang di atas latar hitam. Ketiganya ditemukan dengan MEMBACA PIKSEL kolom selebar 4 px, bukan dengan mata |
| **Kartu komparasi diperbesar 1,55x pada RUTENYA** | Gambar dan lapisan heksagonnya diskala BERSAMA dalam satu pembungkus — menskala salah satunya menggeser seluruh kisi terhadap petanya tanpa satu pun galat. Titik pusatnya tengah rute, bukan tengah gambar: rute berangkat dari heksagon menuju stasiun, sering di tepi bingkai. Pembandingnya juga diganti dari Dukuh Atas (basemap gelap) ke Manggarai (terang), supaya kartunya berhenti jadi separuh peta bertemu separuh lubang hitam |
| **Tombol yang berpendar mengikuti keadaan akun** | Selama belum ada akun, yang berpendar "Daftar"; sesudah masuk, "Masuk ke peta". Membatalkan keputusan 9 Sep, dan alasannya kuat: peta bisa dibuka siapa pun, jadi mengarahkan mata ke sana lebih dulu menunda satu-satunya langkah yang mengubah apa yang akan ia lihat di sana |
| **Jarak antarbagian gerbang dinaikkan** | py-28/36 → py-40/52. Permintaan pemilik repo |

---

### 11 September 2026, putaran keempat — basemap hidup lagi, dan transisi yang cocok

| Bagian | Bukti |
|---|---|
| **Ubin MAPID hidup kembali sesudah lima hari** | Yang dikira pemadaman ternyata tembok otentikasi: `/`, `/health`, `/styles/`, dan ubinnya sama-sama 401 tanpa kunci. Dengan kunci basemap, `/styles/*` menjawab 200 dan satu ubin z14 di atas Manggarai pulang **397 KB**. Kuncinya juga dibatasi domain — ia menuntut `Referer` yang cocok, jadi `curl` polos tetap ditolak sementara peramban lolos. Diukur di peramban: **24 respons `basemap.mapid.io`, seluruhnya 200, nol gagal** |
| **Kuncinya sampai ke peramban tanpa pernah masuk git** | Berkas gaya di `public/basemap/` tetap bersih; yang membubuhkan kunci `transformRequest` MapLibre — satu kait untuk gaya, TileJSON, ubin, font, dan sprite sekaligus. Nilainya datang dari secret `MAPID_BASEMAP_KEY` lewat GitHub Actions. Dijaga `test_infra.py`, yang dibuktikan berteriak dengan menaruh kunci di `.env.example` lalu memastikan ia gagal |
| **Asersi keamanan dipersempit, bukan dicabut** | `audit-prd.mjs` dulu menuntut nol `key=` di URL mana pun. Sekarang: kunci rahasia tetap terlarang di mana pun, kunci ubin diizinkan HANYA pada host pemiliknya, plus asersi baru bahwa bentuknya memang kunci basemap. **Audit 40/40 untuk pertama kalinya** — asersi ubin yang merah sejak 6 Sep akhirnya hijau karena ubinnya memang hidup |
| **Enam WebP kartu gerbang akhirnya dirender ulang** | Terhalang sejak 6 Sep. Keenamnya lahir dari ubin hidup, 288 KB total, lengkap dengan rute jalan kaki tiap kartu (4–34 menit ke simpulnya). Kartu Dukuh Atas yang sejak 3 Sep memperlihatkan garis heksagon tanpa isian akhirnya benar |
| **Setelan "Nama tempat: Mati" terbukti bekerja** | Sebelumnya tidak bisa diuji sama sekali — tanpa ubin tidak ada label untuk disembunyikan. Terukur sekarang: potret PNG turun 328 KB → 249 KB begitu Off ditekan, yang persis yang terjadi kalau ratusan label teks lenyap |
| **Transisi tema jadi silang-pudar** | Bentuk ketiga, dan dua yang pertama gagal karena alasan yang sama: keduanya punya PENDAPAT. Lingkaran cocok untuk tombol bundar, sapuan mendatar cocok untuk slider tetapi membawa arah. Yang tersisa satu lembar warna tema tujuan yang muncul lalu hilang — nol gerakan, nol arah. Terukur: opasitas 0 → 1 dalam 240 ms, tema ditukar, lalu kembali ke 0 dalam 300 ms |
| **"The basemap failed to load" berhenti muncul untuk galat kita sendiri** | Penangan galatnya melaporkan apa pun yang lewat selama `!isStyleLoaded()` — termasuk `Cannot style non-existing layer` dari efek yang menata layer sepersekian detik sesudah pergantian gaya membongkarnya. Sekarang keluarga galat itu diklasifikasikan lebih dulu dan pergi ke konsol. Diuji dengan mengganti basemap ke Street: nol pita galat, nol galat konsol |
| **Garis di tepi kartu Solutions hilang** | Peta berhenti pada luminansi 160, tepi kartu 215, halaman 242 — tiga bidang rata bertemu dalam dua piksel. Tepinya tidak berubah; POTRETNYA yang membaik sejak ubin hidup. Masker bulu 10 px di pembungkus gambar (bukan di media-nya, supaya atribusi MAPID tidak ikut memudar): sekarang memudar 164 → 249 sebelum bertemu tepinya |

---

### 11 September 2026, putaran kelima — tiga moda rute, tema tanpa tirai, gerbang yang bergerak

| Bagian | Bukti |
|---|---|
| **Sepeda menggantikan motor** | Profil `cycling-regular` di keempat tempatnya: `rute_ors.py --sepeda`, `Literal` endpoint `/hex/{h3}/simpul-terdekat`, kata kerja `simpul_cara_sepeda`, dan `URUTAN_PROFIL` frontend. Motor dulu meminjam jaringan MOBIL dan cuma boleh menyebut jarak; sepeda punya jaringan dan waktu tempuhnya sendiri, jadi `ModaTampil` dan pemetaan moda→profil dicabut. Di peta, pola ketiga: kapsul berderet (titik = jalan kaki, padat = mobil). `motorcycle` sekarang dijawab 422. Dijaga `test_aturan.py::test_profil_rute_sama_di_empat_tempat`, dibuktikan berteriak dengan mencabut sepeda dari frontend lalu dari `KALIMAT` |
| **Rute mobil 175 → 369 dari 708 heksagon** | Depok Baru 127/127, Dukuh Atas 97/97, Harjamukti 18/127 menyusul Bekasi 127/127. Kuota harian ORS habis lagi di heksagon ke-195 (`403 Quota exceeded`); tiga heksagon Harjamukti memang tidak punya jalan mobil dalam 350 m. Sepeda baru 2 heksagon (uji coba). Skripnya sekarang BERHENTI di kuota habis (kode 3) alih-alih mencatat seribu "gagal" |
| **"Safe Bet" → "Safe"** | `KUADRAN.PEMENANG_JELAS.namaEn` dan `LABEL_KUADRAN_EN` bersama. Nol sisa "Safe Bet" di repo |
| **Transisi tema tidak lagi menutup layar** | View Transitions API: halaman lama memudar di atas halaman baru yang sudah hidup, 520 ms. Dibekukan di tengah lewat Web Animations API lalu dipotret: simpangan luminansi 41 → 27 → **18,5 (titik tengah)** → 35 → 38, judul dan tombol terbaca di setiap bingkai; kanvas peta tetap tergambar. Sakelarnya dipotret terpisah, jadi kenopnya meluncur tanpa ikut dipudarkan. Peramban tanpa API: transisi warna CSS selama 580 ms saja |
| **Animasi tutup untuk Pengaturan, Akun, Layer, Kawasan** | Satu kait `useTutupHalus()`. Disampel per bingkai di headless: opasitas 1,00 → 0,47 → 0,00 lalu elemennya dicabut, di keempatnya. Versi pertama mencabutnya di opasitas 0,44-0,57 - lihat jebakan.md |
| **Peta Solutions terang di mode terang** | Peredupan `brightness(0.62)` dibatasi ke tema gelap; kartu GemFinder memakai kembaran `dukuh-atas-terang.webp`. Atribusi MAPID ikut warna peta di belakangnya |
| **Kartu komparasi dibingkai pada rutenya** | Dua potret sendiri (`banding-a/b.webp`, 900×520) dengan SATU zoom (14,94) untuk keduanya: rute 1.832 m di Harjamukti dan 2.651 m di Manggarai, masing-masing hampir selebar petanya. Hanya peta kiri yang dipudarkan (26%, dulu 68% di kedua peta); kolom teks 38% → 31%. Rutenya bertahan sampai 86% siklus, dulu 48% |
| **Latar Solusi dan Ekosistem** | Solusi: tanda silang lembar peta (topeng SVG, warna dari token tema). Ekosistem: cincin kawasan jangkau konsentris di puncak tulang punggungnya, di atas dua cahaya yang sudah ada |
| **Kemunculan bagian Latar belakang dan Solusi** | Label tersapu, judul kata demi kata naik dari topengnya, paragraf baris demi baris, garis rambut tumbuh, pelat terbuka dari sisinya dengan gambar mendarat dari 1,12x, panel penutup terbuka dari bawah; kartu Solusi terbuka dari tengah susunan, petanya mendarat dari 1,3x, lalu satu kilau menyapu sekali. Semua sekali jalan, `expo.out`. Terukur: nol `clip-path` tertinggal, nol kelebihan lebar saat digulir di 390 px dan 1024 px, nol galat konsol |
| **Bilah atas gerbang diperbesar** | 54 → 66 px, lebar 72 → 76rem (sejajar kisi bento), tanda heksagon di depan nama. `-mt` hero ikut: 86,4 px di sm ke atas, 72 px di ponsel - diukur, bukan ditebak |
| **Nama akun terbaca di gerbang terang** | Token aplikasi dikembalikan ke versi terangnya di bawah `.gerbang[data-tema='terang']`. Terukur dengan sesi dipalsukan di peramban headless: rgb(22,33,28) di atas bilah putih |
| **Panel kaca peta buram lagi di terbitan publik** | Bug produksi yang tidak pernah terlihat di dev - lihat jebakan.md. `backdropFilter` di `vite preview`: "none" → "blur(26px) saturate(1.8)" |
| **Basemap pertama sama di dev dan produksi** | Tema terang → Dasar, gelap → Gelap, diukur dari gaya yang diminta di KEDUANYA |

---

### 11 September 2026, putaran keenam — bilah gerbang: papan nama yang hidup, dan menu pengaturan

| Bagian | Bukti |
|---|---|
| **Nama di bilah gerbang melenting dan berwarna, seperti di peta** | Dulu teks polos. Sekarang `PapanNama` yang SAMA dengan bilah peta - bukan tiruan - lewat `sebagai="span"` yang baru, karena ia duduk di dalam tombol kembali-ke-atas; tombolnya ikut membawa `aria-label`. Disapu kursor di headless: kesepuluh huruf mengambil warna dan melenting, lalu luntur sendiri sesudah kursor pergi, dan klik tetap menggulir ke atas. Dibuktikan merah dengan mengembalikan teks polos |
| **Bahasa dan tema pindah ke dalam menu pengaturan** | Sakelar bahasa di bilah dan sakelar tema di tengah bawah hero dicabut. Yang berdiri di bilah sekarang gerigi `MenuPengaturan varian="gerbang"` - komponen yang sama dengan peta: Bahasa, Tampilan, Tentang kami, Kontak. "Nama tempat" tidak ikut; itu setelan basemap. Variannya cuma bahan (`.g-pil`) dan ukuran (44 px, setinggi tombol akun). Diuji: buka, tutup lewat klik luar / Enter / Escape, ganti bahasa dari menu (label tombolnya ikut jadi "Settings"), ganti tema dari menu (`view-transition-name` dibersihkan sesudahnya). Prop `gelap` `SakelarBahasa` dan token `--sb-*` di `.gerbang` ikut dicabut: tidak ada lagi yang membacanya |
| **Dialog Tentang kami / Kontak terbuka DI ATAS gerbang** | `z-[60]` → `z-[80]`, lapisan yang sama dengan `Tirai`. Tanpa itu dialognya terbuka di belakang gerbang `z-[70]` - nol galat, cuma klik yang tidak menghasilkan apa-apa. Tirainya `bg-ink/30` → `bg-black/55`, karena krem-putih 30% di tema gelap memutihkan halaman hitam. Dijaga `elementFromPoint` di tengah layar, dibuktikan merah dengan `z-[60]` |
| **Pesan pengembang di dialog Kontak diganti** | "Isi nilainya di IDENTITAS pada frontend/src/config.ts." → "Kontak tim belum dicantumkan.", dan kalimatnya hanya tampil selama KEEMPAT kontak di `IDENTITAS` kosong - diturunkan dari nilai yang sama dengan barisnya |
| **Menu di atas jurang pada tema terang terbaca lagi** | Baris menu Pengaturan dan nama di kepala menu Akun mewarisi tinta gelap di atas kaca gelap: kontras 1,03 → 15,84. Lihat jebakan.md |
| **Nol regresi di tempat lain** | `tsc` 0; `oxlint` 22 peringatan, identik dengan HEAD; `test_infra` 58/58, `test_aturan` 58/58; `audit-prd.mjs` 33/33 tanpa SANDI; build produksi 9 chunk seperti HEAD, selisih ratusan byte. 60 asersi Playwright hijau di dev DAN `vite preview` - 1440/640/390 px, kedua tema, kedua bahasa, tamu dan sesi yang dipalsukan di peramban (tanpa akun sungguhan). Nol gulir mendatar di 390 px: isi bilah 297 px di bilah 358 px |

---

### 11 September 2026, putaran ketujuh — kilatan putih, bahasa yang tertinggal, dan tab yang meluncur

| Bagian | Bukti |
|---|---|
| **Kilatan putih gerbang ↔ peta hilang** | Dilaporkan pemilik repo. Direkam per bingkai (CDP screencast + pencatat elemen teratas tiap bingkai), empat skenario × dua tema, di dev dan build produksi. Sebelum: masuk peta pertama kali luminansi **241**, pulang di sesi yang dimulai di peta **218**, keduanya 100% piksel terang, pelakunya dua fallback `Suspense` bermint pucat; di tema terang tirai pulang jadi kilatan hitam (237 → 14). Sesudah: gelap tidak pernah melewati **30**, terang tidak pernah turun ke gelap. Fallback gerbang memakai latar `.gerbang` itu sendiri, tirai pulang ikut tema dan menunggu chunk gerbang tiba |
| **Kalimat backend benar-benar ikut bahasa** | `lang` di `<html>` kini ditulis sebelum render dan sebelum `setBahasa`, bukan di efek leluhur. Terukur lewat jaringan: sesudah menekan EN, kelima permintaan ulang membawa `bahasa=en` (sebelumnya nol); muat awal dengan EN tersimpan juga, di build produksi - tempat bug ini sebenarnya muncul, karena StrictMode di dev menutupinya |
| **Tab Untuk Anda / Daftar lokasi / Loconomics AI dengan transisi** | Satu penunjuk yang meluncur (`grid-cols-3` + `translateX`, tanpa pengukuran) berisi salinan label terang yang digeser berlawanan arah, jadi warna berganti persis di tepinya. Isi tab masuk dari sisi yang searah penunjuk, fade-through 120 ms keluar lalu 280 ms masuk. Diperiksa dengan membekukan transisinya di 0/60/120/180/260/540 ms di kedua tema: nol lapisan teks yang saling menembus. Gerak-dikurangi: pertukaran seketika. Label "Loconomics AI" berhenti terlipat dua baris di layar 360 px |
| **Kembali ke tab terasa seketika** | Pane dipasang saat pertama dibuka lalu tetap hidup: nol permintaan `/skor/ranking` baru, nol layar tunggu, posisi gulir dan ketikan di Loconomics AI bertahan, detail heksagon tidak diminta ulang. Wadahnya `overflow: clip` - dibuktikan: dengan `overflow: hidden`, `scrollIntoView` di pane AI yang tersembunyi menggeser seluruh wadah 16 px |
| **Tanpa membuat klik tab berat** | Pane yang tetap hidup semula membuat bingkai pertama sesudah klik **350 ms** di dev (kode sebelumnya 33 ms). `memo` pada keempat komponen pane + empat prop yang distabilkan: **58 ms** di dev, **53 ms** di produksi |
| **Nol regresi di tempat lain** | `tsc` 0; `oxlint` 22 peringatan, sama dengan sebelum sesi; `test_infra` 58/58, `test_aturan` 58/58; `audit-prd.mjs` 33/33 tanpa SANDI; bundel pertama +1,5 kB, `Rekomendasi` tetap chunk malas. 40 asersi transisi tab hijau di dev DAN build produksi (1440/390/360 px, gerak-dikurangi), asersi arahnya dibuktikan merah dengan menyuntikkan `transition: none`; 60 asersi pengaturan gerbang hijau di build produksi |

---

### 11 September 2026, putaran kedelapan — tim di baliknya: kartu potret, warna per orang

| Bagian | Bukti |
|---|---|
| **Tiga di atas, dua di tengah bawah** | Kisi enam kolom di `lg`, tiap kartu dua kolom, dan baris sisa berangkat dari kolom 2 - lewat `kelasSelTim()`, yang ditulis untuk jumlah orang berapa pun. Terukur: di 1440 dan 1024 px celah kiri dan kanan baris bawah sama (201 px di 1440); di 820 px 2-2-1 dengan kartu kelima di tengah dan selebar yang lain; di 390 px satu kolom; nol gulir mendatar di keempatnya. Kelima kartu setinggi (`auto-rows-fr`). Dibuktikan merah dengan mencabut `col-start` (kiri 0, kanan 401) |
| **Kartu memanjang ke bawah** | 381 × 491 px di 1440 (sebelumnya 381 × 214). Kepala kartu adalah panggung dengan terowongan heksagon - bentuk yang sama dengan terowongan jurang di atasnya - dan lencana heksagon berinisial; badannya peran, nama, garis berwarna, dan kalimat kerja |
| **Satu warna identitas per orang** | `PENDIRI[].rona` di config.ts: teal (Ajis), ungu (Ukas), fuchsia (Wily), langit (Fijar), emas (Irvan, ketua) - masing-masing dengan warna pendamping. Cahayanya hanyut pelan lewat `transform` saja, tiap kartu dari titik waktu yang berbeda, dan berhenti sendiri saat bagian tim di luar layar. Dicatat sebagai pengecualian ketiga dari "warna jenuh hanya berarti kuadran": yang diwarnai identitas, bukan data. Diuji: lima warna berbeda, kelimanya bergerak dan tidak serempak, berhenti di luar layar, diam saat gerak-dikurangi |
| **Efek angkat saat disorot akhirnya bekerja** | Sebelumnya 0,00 px sejak animasi masuk dipasang - transform sebaris GSAP mengalahkan `:hover`. Sekarang 6 px, dengan tepi berwarna orangnya dan cahaya yang menguat. Lihat jebakan.md |
| **Badan kartu pekat** | Garis terowongan jurang dulu melintas di atas teks deskripsi, dan batas kepala-badan terbaca sebagai sambungan. Terukur sesudahnya: selisih luminansi di batas itu 0,5 - juga saat kartu disorot, sesudah warna badan saat hover dicabut (versi itu memberi selisih 4,3) |
| **Biaya gulir yang tersisa, dinyatakan** | Build produksi headless, berselang-seling: kartu lama 189 bingkai/4 dtk, kartu baru 150. Sebagian besar dari kartu yang lebih tinggi dan berwarna; ~7% dari lapisan cahaya yang bergerak, dan tiga penangkalnya dibantah pengukuran - lihat jebakan.md |
| **Nama lengkap, prodi, dan angkatan kelima orang** | Diminta pemilik repo; kolom `namaLengkap`, `prodi`, `angkatan` di `PENDIRI`. Diisi dari data yang DIKETIK pemilik repo, bukan dibaca dari scan KTM di folder registrasi Drive - unduhannya ditolak pengaman izin sebagai dokumen data pribadi, dan yang boleh keluar ke halaman publik memang cuma ketiga kolom ini (NIM dan foto tidak). Inisial lencana ikut nama lengkap (huruf pertama nama depan dan belakang). Kolom yang kosong tidak dikarang: kartu kembali ke nama panggilan dan melewati baris prodinya. Nama prodi tidak diterjemahkan; "Angkatan" jadi "Class of". Diuji: kelima nama, prodi, angkatan, inisial, badge ketua, dan versi Inggrisnya - 51 asersi tim hijau di dev dan build produksi; tata letak juga diuji dengan isian palsu yang sengaja panjang sebelum data aslinya ada |
| **Nol regresi di tempat lain** | `tsc` 0; `oxlint` sama dengan sebelum sesi; `test_infra` 58/58, `test_aturan` 58/58; `audit-prd.mjs` 33/33 (satu putaran sebelumnya 32/33 pada asersi saringan kuadran peta - tidak berulang, dan tidak menyentuh berkas yang diubah); 51 asersi tim hijau di dev DAN build produksi, lima penjaganya dibuktikan merah lewat sabotase; 60 asersi pengaturan gerbang hijau di build produksi |

---

### Belum dikerjakan — di sinilah pekerjaan berikutnya

| Hal | Yang menghalangi | Kalau sudah ada, kerjakan |
|---|---|---|
| **`KOLOM_*_GO` masih kosong** di `pipeline/config.py` | CSV misi asli belum diunduh | **Ini yang pertama.** Cocokkan nama kolom, lalu `s1`–`s2` bisa jalan |
| Badan `s3` dan sisa `s4` | Menunggu `LLM_API_KEY` dan keputusan penyedia vision | Docstring-nya sudah memuat keputusan yang diambil — ikuti, jangan analisis ulang. `s4::profil_jam`, `belanja_per_jam`, dan `harga_sewa_per_m2` SUDAH jalan dan teruji. **`s5` sudah SELESAI** (29 Agu 2026) — Random Forest + spatial k-fold, 34 uji, dan menolak jalan sampai ground truth-nya cukup |
| **Kunci Gemini pameran wajib DICABUT sesudah 4 Sep 2026** | Kunci sempat lewat percakapan sesi ini - risiko keamanan standar untuk kunci yang pernah tertulis di teks | Buat kunci Gemini baru di Google AI Studio, cabut yang lama, perbarui `LLM_API_KEY` di `backend/.env` DAN di Azure App Settings. Atau kembali ke `LLM_PROVIDER=anthropic` kalau sudah ada kunci Claude tim |
| **`tanggal` kosong di 691 dari 691 titik API** — tetapi jamnya ada di dalam FOTO | Struk Go tidak mengembalikan waktu transaksi sama sekali. Sekilas mematikan B01-B05, D11, P06. Diperiksa langsung: struk kertas mencetak `Date: 15-08-2026 07:05` + `Grand Total: 105.000`; tangkapan layar GoPay memuat `Rp18.000` dengan jam di bilah status | A2 mengekstrak **nominal + tanggal + jam** sekaligus, jadi `LLM_API_KEY` membuka 12 variabel — termasuk **Commuter Clock, PriceLens, dan sumbu prestise**. Itu pembuka terbesar yang tersisa |
| **Rute mobil baru 369 dari 708 heksagon, sepeda baru 2** | **Kuota harian OpenRouteService habis** dua kali: 9 Sep (175) dan 11 Sep (369, `403 Quota exceeded`). Sudah lengkap: Bekasi, Depok Baru, Dukuh Atas BNI. Harjamukti 18/127, Manggarai dan Tanah Abang belum sama sekali. Tiga heksagon Harjamukti tidak punya jalan mobil dalam 350 m dan akan tetap tanpa rute mobil | `cd pipeline && python rute_ors.py --mobil`, lalu `python rute_ors.py --sepeda`. Keduanya melanjutkan sendiri dan BERHENTI dengan kode 3 kalau kuotanya habis lagi. Yang belum ditarik tidak berbohong di layar: tombolnya mati dan bertulis "belum ditarik" |
| **Laporan PDF dan jawaban Konsultan AI masih Indonesia saja** | Keduanya sengaja ditinggalkan saat kalimat backend dibuat dua bahasa. PDF memuat ~80 label dan merupakan dokumen resmi yang dicetak dan dibagikan; jawaban AI datang dari model dan menuntut varian prompt-nya sendiri, bukan tabel kalimat | Katalognya sudah berdiri: `KALIMAT` + `pilih()` di `core/aturan.py`, dan `LABEL_KUADRAN_EN`/`ARTI_INDEKS_EN` yang dipakai PDF sudah ada. Yang tersisa menyalurkan `bahasa` ke `_laporan_pdf()` dan menambah satu berkas prompt Inggris di `pipeline/prompts/` |
| Mode gelap | Belum dikerjakan, sengaja | Basemap gelap sudah bisa dipilih; chrome-nya masih terang |
| Sumber NJOP (P01, P02) | **Tidak ada sumber terbukanya** — dicari sampai habis 29 Agu 2026, lihat `docs/data.md` 10.9 | Jalurnya permintaan resmi ke Bapenda DKI, bukan API |
| RDTR di luar DKI (L01–L03) | **Tidak ada** — Kota Depok dan Kota Bekasi terkonfirmasi tanpa RDTR lewat dua indeks GISTARU yang berbeda, lihat `docs/data.md` 10.10 | Berhenti di 328/708 heksagon, dan itu batas sumbernya |
| **Survei lapangan 30 heksagon** | Tim survei. Ini sekarang penghalang TERBESAR: 12 dari 18 variabel kosong hanya bisa diisi orang yang berdiri di lokasinya, dan 23 dari 24 heksagon berskor tertinggi belum pernah disurvei | Daftar target berkoordinat ada di `docs/data.md` bagian 11. Setelah masuk: `s5_impute` berhenti menolak, lalu **ulangi uji sensitivitas** dan laporkan apa adanya |
| D06 `ridership_proksi` | Bukan data yang kurang, melainkan SATUAN. BPS hanya menerbitkan per MODA (MRT 3,59 juta/bulan; LRT 101 ribu/bulan), bukan per stasiun. KAI Commuter menyebut angka stasiun di siaran pers dengan metrik yang berbeda-beda | **Keputusan pemilik repo**: satuan mana yang dipakai untuk membandingkan KRL, MRT, dan LRT dalam satu kolom |
| GeoJSON heksagon statis untuk Cloudflare | — | Mitigasi Render free tier. Gaya basemap SUDAH statis (`frontend/public/basemap/`); heksagonnya belum — `s7_publish.py --ekspor` sudah membangkitkan berkasnya, tinggal disajikan |

Kerangka `s1`–`s4` **bukan TODO kosong** (`s5` sudah terisi). Setiap docstring memuat keputusan yang
sudah diambil — ambang, urutan, jebakan yang harus dihindari. Isi badannya;
jangan mengulang analisisnya.
---

## Status per 13 September 2026 (finalisasi)

Empat pekerjaan besar sejak catatan di atas. Yang menyatukan keempatnya satu
hal: **membuat batas antara yang diukur dan yang diperkirakan bisa dilihat dari
luar**, bukan cuma dipercaya.

### Selesai dan terverifikasi

| Bagian | Bukti |
|---|---|
| **Blok res-10 di dalam heksagon** | Jawaban atas keluhan "heksagonnya terlalu besar untuk membandingkan dua sisi jalan". Tiap heksagon dipecah jadi TUJUH anak (±130 m) di tabel BARU `blok_heksagon` — 4.956 baris, nol kolom lama berubah, dan peta bekerja persis seperti sebelumnya kalau fiturnya tidak dipanggil. Enam indikator per blok dari sumber yang sudah ada (matriks ORS, jalan OSM, RDTR), `skor_per_kelas` untuk delapan kelas usaha sehingga pesaing sekelas menurunkan peringkat untuk kelas ITU saja. Diverifikasi di peramban: 19 asersi, dua bahasa, 390 px, selamat dari pergantian gaya basemap |
| **Lima gaya basemap + mode 3D** | Gaya kelima (satelit) dimuat langsung dari MAPID, tidak disalin ke repo — alasannya di `CLAUDE.md` aturan 6. Gedung 3D memakai `fill-extrusion` dari sumber ubin MAPID yang sama. Diverifikasi lima gaya × 2D/3D di peramban, termasuk klik heksagon dalam keadaan miring |
| **Serah terima tim data & AI** | 40.288 heksagon res-9 dari repo tim; seluruh 708 heksagon kita ada di dalamnya. D02 diterima sebagai DATA (708/708, cakupan variabel 25 → 26 dari 43, dan tidak menggeser satu pun skor karena D02 tidak ada di satu pun `BOBOT_*`). D10 dan B07 masuk `hex_perkiraan` sebagai PERKIRAAN. Sembilan kolom lain sengaja DITOLAK, alasan per kolom di `KOLOM_TIM_AI_DILEWATI` |
| **Uji silang model tim terhadap ukuran kita** | R² 0,62 / 0,57 yang dilaporkan diukur terhadap label yang 96,9%-nya sintetis. Diuji ulang terhadap 12 heksagon yang punya pengamatan misi MAPID sungguhan: **B07 meleset Rp14.580 (41%), D10 meleset 2,34 pada skala 1–3** — lima kali lebih besar. Angkanya disimpan di `hex_perkiraan.rincian` dan ikut tertulis di tiap kalimat perkiraan di layar |
| **Layar "Sumber data"** | Pengaturan → Sumber data. Daftar RESMI vs PERKIRAAN, cakupan per sumber, batasan, dan keempat temuan. Seluruh isinya dari `lib/ringkasan-data.ts` yang dibangkitkan pipeline — nol angka diketik tangan. Ikut menutup lubang lama: dari empat nilai yang dibangkitkan berkas itu, hanya SUMBER yang pernah dipakai komponen |
| **OCR foto misi (`s3_extract.py`)** | Badan modul yang sejak lama cuma kerangka. Gemini vision lewat REST, prompt dari berkas (aturan 7), cache per SHA-1 URL foto sehingga lari ulang tidak membayar dua kali |
| **Commuter Clock berhenti kosong** | A2 membaca 310 struk (294 dipakai, 6 perlu review, 0 gagal). Tujuh heksagon kita punya profil jam SUNGGUHAN, dan 454 heksagon punya pola jam tingkat KAWASAN sebagai perkiraan berlabel. Cakupan variabel **25 → 34 dari 43** sepanjang sesi ini |
| **Kawasan jangkau lengkap** | `ISOCHRONE_MENIT` diperluas jadi lima pita 3 Sep 2026 di ketiga tempat yang menyebutnya, tetapi pipelinenya tidak pernah dijalankan ulang — basis data menyimpan tiga selama sepuluh hari. Ditangkap `smoke_api` (yang memang berjalan atas data produksi), ditambal, 114 asersi hijau |
| **Terbit dan terverifikasi di publik** | GitHub Pages DAN Cloudflare Pages, keduanya diuji dengan mengKLIK petanya — bukan `curl` halaman depan. 9 asersi per situs: peta tergambar, backend Azure menjawab, panel detail terisi, bedah blok dan layar Sumber Data hidup, nol galat konsol |
| **Enam jebakan baru tercatat** | Termasuk dua yang gagalnya paling diam: sprite `maputnik.github.io` yang dilaporkan sebagai kegagalan basemap, dan kalimat Indonesia yang tinggal di nilai bawaan parameter `<Memuat/>` sehingga lolos `tsc` DAN kedua uji |

### Temuan yang mengubah rencana

**Spanduk "DIKONTRAKAN" hampir tidak pernah memuat harga.** Dari 19 spanduk
pertama yang dibaca A1, 18 hanya memuat nomor telepon. Modelnya membaca dengan
benar; yang tidak ada angkanya. Artinya A1 bukan jalan menuju P05 — sumber
harga sewa harus dicari di tempat lain, dan `perlu_review` sudah menahan
hasilnya supaya tidak ada angka karangan yang masuk tabel observasi.

Konsekuensinya A2 (struk → nominal + jam) naik jadi pekerjaan OCR yang paling
berharga: ia satu-satunya jalan ke B01–B04 yang mengisi Commuter Clock, dan
Commuter Clock adalah fitur BERBAYAR yang tabelnya sekarang nol baris.

### Catatan operasional yang menentukan menjelang penjurian

**OCR dan Konsultan AI berbagi satu kunci Gemini, jadi satu jatah harian.**
Membaca 330 foto struk menghabiskannya pada 13 Sep 2026, dan Konsultan AI di
situs publik ikut mati sampai jatahnya pulih (tengah malam Pasifik). Jangan
menjalankan pekerjaan AI **batch** menjelang penjurian — yang mati duluan
justru yang dilihat orang.

Sejak itu `/ai/status` ikut jujur: ia melaporkan `siap: false` dengan kalimat
yang menyebut jatah habis, alih-alih mengundang pertanyaan yang sudah pasti
gagal. Kalau perlu jaminan penuh, pisahkan kuncinya — satu untuk batch, satu
untuk produk.

`ai_call_logs` sekarang benar-benar terisi: **330 panggilan A2, 6 perlu
tinjauan manusia (1,8%), keyakinan rata-rata 0,948, $0,36**. Itu jawaban
berangka untuk pertanyaan juri "berapa banyak yang perlu koreksi manusia?".

### Yang menghalangi pekerjaan berikutnya

| Hal | Yang menghalangi | Kalau sudah ada, kerjakan |
|---|---|---|
| **B01–B04 baru 7 dari 708** | Bukan OCR-nya yang kurang — 330 dari 462 struk sudah terbaca, tetapi hanya 9 yang jatuh DI DALAM 708 heksagon kita. Misi disebar se-Jabodetabek. Sisa 132 foto berhenti di **kuota Gemini (HTTP 429)**, bukan di bug | Lanjutkan `python s3_extract.py --struk --pekerja 10` besok — cache per SHA-1 membuat lari ulang tidak membayar ulang untuk 330 yang sudah dibaca. Tetapi yang benar-benar menambah cakupan B01–B04 bukan lebih banyak pembacaan foto, melainkan **survei struk DI DALAM kawasan pilot** |
| **P05 harga sewa** | Sumbernya hilang: spanduk tidak memuat harga (lihat temuan di atas) | Cari sumber lain, atau nyatakan kosong apa adanya. JANGAN mengisinya dari perkiraan model — batas itu yang sedang dijaga seluruh pekerjaan sesi ini |
| Rute mobil & sepeda | Kuota harian ORS | Tidak berubah dari catatan sebelumnya |
